import React from "react";

const ManualEntry = ({ modelState, setModelState, fileData, setFileData }) => {
	return (
		<button className="bottom-button">
			Edit Data
		</button>
	);
}

export default ManualEntry;